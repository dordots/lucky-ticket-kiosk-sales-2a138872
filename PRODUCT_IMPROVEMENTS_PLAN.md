# תכנון שיפורים למוצר - כרטיסי מזל

## סקירה כללית
מסמך זה מכיל תכנון מפורט ומשימות לביצוע כל השיפורים והשינויים שנדרשו בהערות.

---

## 1. מלאי (Inventory)

### 1.1 הוספת סוג כרטיס ✅
**תיאור:** הוספת אפשרות להגדיר סוג כרטיס (למשל: כרטיסי מפעל הפיס הקיימים vs כרטיסים מותאמים אישית)

**משימות:**
- [x] הוספת שדה `ticket_category` ל-TicketType (enum: 'pais', 'custom')
- [x] עדכון טופס הוספת/עריכת כרטיס עם בחירת סוג
- [x] עדכון תצוגת המלאי להצגת סוג הכרטיס
- [x] הוספת פילטר לפי סוג כרטיס

**קבצים לשינוי:**
- `src/pages/Inventory.jsx`
- `src/firebase/services/ticketTypes.js`
- `src/api/entities.js` (אם נדרש)

**הושלם:**
- ✅ יצירת סקריפט `scripts/seed-pais-tickets.js` להוספת כל כרטיסי מפעל הפיס
- ✅ הסקריפט הוריץ בהצלחה והוסיף 44 כרטיסים עם `ticket_category: 'pais'`
- ✅ כל הכרטיסים מוגדרים עם `quantity: 0` (לא במלאי) ו-`is_active: true` (פעילים)
- ✅ הוספת פקודה `npm run seed-pais` ל-package.json
- ✅ עדכון טופס הוספת/עריכת כרטיס עם בחירת סוג (מותאם אישית / מפעל הפיס)
- ✅ עדכון תצוגת המלאי - תג "מפעל הפיס" מופיע ליד כרטיסי מפעל הפיס
- ✅ הוספת פילטר לפי סוג כרטיס (כל הסוגים / מפעל הפיס / מותאם אישית)
- ✅ הגנה: לא ניתן לשנות סוג כרטיס של מפעל הפיס בעריכה

**הרצת הסקריפט:**
```bash
npm run seed-pais
```

**תוצאות:**
- 44 כרטיסים נוצרו בהצלחה
- 2 כרטיסים דולגו (כבר קיימים: קזינו, רולטה)

---

### 1.2 הנפקת קוד אוטומטית ✅
**תיאור:** המערכת תצטרך להנפיק קוד אוטומטית לכל כרטיס, לא ניתן לבחירה ע"י המשתמש

**משימות:**
- [x] יצירת פונקציה ליצירת קוד ייחודי (UUID קצר או מספר סידורי)
- [x] הסרת שדה הקוד מהטופס (או הפיכתו ל-readonly)
- [x] עדכון לוגיקת יצירת כרטיס להנפקת קוד אוטומטית
- [x] בדיקה שהקוד ייחודי (validation)

**קבצים לשינוי:**
- `src/pages/Inventory.jsx` - הסרת שדה קוד מהטופס, הוספת לוגיקה ליצירת קוד
- `src/firebase/services/ticketTypes.js` - הוספת פונקציה ליצירת קוד ייחודי

**הושלם:**
- ✅ יצירת פונקציה `generateUniqueCode` ב-`ticketTypes.js`
- ✅ הקוד נוצר אוטומטית לפי קטגוריה: `PAIS-XXXXX` או `CUST-XXXXX`
- ✅ בדיקת ייחודיות - הפונקציה בודקת שהקוד לא קיים לפני החזרה
- ✅ שדה הקוד בטופס הופך ל-readonly/disabled
- ✅ בעת יצירת כרטיס חדש - הקוד נוצר אוטומטית
- ✅ בעת עריכת כרטיס קיים - הקוד נשאר ללא שינוי

**אלגוריתם:**
```javascript
// קוד ייחודי עם prefix לפי קטגוריה
// PAIS-XXXXX עבור כרטיסי מפעל הפיס
// CUST-XXXXX עבור כרטיסים מותאמים אישית
// בודק ייחודיות עד 10 ניסיונות, אחרת משתמש ב-timestamp
```

---

### 1.3 הוספת כינוי (Nickname) לכרטיס ✅
**תיאור:** אפשרות להוסיף כינוי לכרטיס שיופיע ליד השם המקורי

**משימות:**
- [x] הוספת שדה `nickname` ל-TicketType
- [x] עדכון טופס הוספת/עריכת כרטיס עם שדה כינוי (אופציונלי)
- [x] עדכון תצוגת המלאי להצגת כינוי ליד השם (אם קיים)
- [x] עדכון תצוגת המכירה להצגת כינוי
- [x] עדכון חיפוש לכלול גם כינוי

**הושלם:**
- ✅ הוספת שדה `nickname` ל-formData ול-handleSubmit
- ✅ הוספת שדה כינוי בטופס (אופציונלי) עם הסבר
- ✅ תצוגת כינוי בתצוגת המלאי - מופיע מתחת לשם עם גרשיים
- ✅ תצוגת כינוי בתצוגת המכירה (TicketGrid) - מופיע מתחת לשם
- ✅ עדכון החיפוש ב-Inventory וב-SellerPOS לכלול גם כינוי

**קבצים לשינוי:**
- `src/pages/Inventory.jsx` - הוספת שדה כינוי בטופס ותצוגה
- `src/components/pos/TicketGrid.jsx` - הצגת כינוי
- `src/pages/SellerPOS.jsx` - עדכון חיפוש

**תצוגה מוצעת:**
```
שם: אואזיס
כינוי: מטוסים
```

---

### 1.4 ערכים מוגדרים מראש (מחיר, כמות, תמונה) ✅
**תיאור:** להכניס מראש מחיר וכמות יחידות בחבילה, עם אפשרות עריכה לכמות בלבד (לא למחיר)

**משימות:**
- [x] הוספת שדה `default_quantity_per_package` ל-TicketType
- [x] עדכון טופס הוספת כרטיס - שדה מחיר יהיה read-only בעריכה
- [x] הוספת לוגיקה להצגת מחיר כ-read-only בעריכה
- [x] שדה כמות יהיה ניתן לעריכה תמיד
- [x] הוספת הערה/הסבר על זכויות יוצרים בתמונה

**הושלם:**
- ✅ הוספת שדה `default_quantity_per_package` ל-formData ול-handleSubmit
- ✅ שדה מחיר הופך ל-readonly/disabled בעריכה (לא ביצירה)
- ✅ הוספת הודעה "מחיר לא ניתן לשינוי" בעריכה
- ✅ שדה כמות במלאי תמיד ניתן לעריכה
- ✅ הוספת שדה "כמות יחידות בחבילה" עם הסבר
- ✅ הוספת אזהרה על זכויות יוצרים בשדה התמונה (תיבה צהובה עם הסבר)

**קבצים לשינוי:**
- `src/pages/Inventory.jsx` - עדכון טופס עם read-only למחיר בעריכה
- `src/firebase/services/ticketTypes.js` - הוספת שדה חדש

**הערה על תמונות:**
- להוסיף אזהרה/הערה בטופס על זכויות יוצרים
- לשקול שימוש ב-URL חיצוני או העלאת תמונות לשרת

---

### 1.5 סטטיסטיקות מלאי (פעיל/לא פעיל) ✅
**תיאור:** הצגת סטטיסטיקות: כמה כרטיסים סה"כ, כמה פעילים, כמה לא פעילים

**משימות:**
- [x] יצירת קומפוננטה `InventoryStats` או הוספה ל-`Inventory.jsx`
- [x] חישוב סטטיסטיקות: סה"כ, פעילים, לא פעילים
- [x] הצגת הסטטיסטיקות בחלק העליון של דף המלאי
- [x] עיצוב כ-Cards עם אייקונים
- [x] הוספת פילטר מתקדם עם אפשרויות נוספות

**הושלם:**
- ✅ יצירת 3 כרטיסי סטטיסטיקה: סה"כ כרטיסים, כרטיסים פעילים, לא פעילים
- ✅ עיצוב עם gradient וצבעים שונים לכל כרטיס
- ✅ חישוב דינמי של הסטטיסטיקות
- ✅ הוספת פילטר מתקדם עם אפשרויות:
  - פילטר לפי סטטוס (כל הסטטוסים / פעיל / לא פעיל)
  - פילטר לפי מצב מלאי (במלאי / מלאי נמוך / אזל מהמלאי)
  - פילטר לפי טווח מחיר (₪0-25 / ₪25-50 / ₪50-100 / ₪100+)
- ✅ כפתור "פילטרים נוספים" עם אינדיקטור כשפילטרים פעילים
- ✅ אפשרות לנקות את כל הפילטרים בבת אחת
- ✅ הפילטר המתקדם ניתן להרחבה/צמצום

**קבצים לשינוי:**
- `src/pages/Inventory.jsx` - הוספת סטטיסטיקות בחלק העליון

**תצוגה מוצעת:**
```
┌─────────────┬─────────────┬─────────────┐
│ סה"כ כרטיסים │ כרטיסים פעילים │ לא פעילים │
│     50      │     45      │      5      │
└─────────────┴─────────────┴─────────────┘
```

---

## 2. מכירה (POS)

### 2.1 שינוי שם העמוד ✅
**תיאור:** לשנות את שם העמוד מ-"SellerPOS" ל-"דף מכירה" / "עמוד מכירה" / "ראשי"

**משימות:**
- [x] עדכון כותרת העמוד ב-`SellerPOS.jsx`
- [x] עדכון כותרת ב-`Layout.jsx` / תפריט ניווט (אם קיים)
- [x] עדכון כל ההפניות/קישורים לעמוד זה

**קבצים לשינוי:**
- `src/pages/SellerPOS.jsx` - שינוי כותרת
- `src/pages/Layout.jsx` - עדכון תפריט (אם קיים)
- `src/pages/index.jsx` - עדכון routes (אם נדרש)

**הושלם:**
- ✅ שינוי כותרת Header ב-SellerPOS.jsx מ-"כרטיסי מזל" ל-"דף מכירה"
- ✅ עדכון תפריט ניווט ב-Layout.jsx מ-"מכירה" ל-"דף מכירה"

---

### 2.2 הוספת רובריקות מחיר ✅
**תיאור:** להוסיף מעל החיפוש רובריקות: כל הכרטיסים / 5-25 / 30-50 / 50-100

**משימות:**
- [x] יצירת קומפוננטה `PriceFilter` או הוספה ל-`SellerPOS.jsx`
- [x] הוספת כפתורי פילטר מעל שדה החיפוש
- [x] הוספת לוגיקת פילטרציה לפי טווח מחירים
- [x] עדכון `filteredTickets` לכלול גם פילטר מחיר

**קבצים לשינוי:**
- `src/pages/SellerPOS.jsx` - הוספת פילטר מחיר

**הושלם:**
- ✅ הוספת 4 כפתורי פילטר מחיר מעל החיפוש
- ✅ כפתורים: כל הכרטיסים / ₪5-25 / ₪30-50 / ₪50-100
- ✅ עיצוב: כפתור פעיל עם רקע כחול, לא פעיל עם outline
- ✅ לוגיקת פילטרציה לפי טווח מחירים
- ✅ הפילטר עובד יחד עם החיפוש (AND logic)

**תצוגה:**
```
[כל הכרטיסים] [₪5-25] [₪30-50] [₪50-100]
[חיפוש כרטיס...]
```

---

## 3. לוח בקרה (Dashboard)

### 3.1 הסבר על "מלאי קריטי" ✅
**תיאור:** להוסיף הסבר או tooltip על מה זה "מלאי קריטי"

**משימות:**
- [x] הוספת tooltip או icon עם הסבר ב-`StatsCard` של מלאי קריטי
- [x] הוספת הסבר: "כרטיסים שהמלאי שלהם נמוך מסף ההתראה"

**קבצים לשינוי:**
- `src/pages/Dashboard.jsx` - הוספת tooltip/הסבר
- `src/components/dashboard/StatsCard.jsx` - תמיכה ב-tooltip

**הושלם:**
- ✅ הוספת תמיכה ב-tooltip ל-StatsCard
- ✅ הוספת אייקון Info ליד כותרת "מלאי קריטי"
- ✅ הוספת tooltip עם הסבר: "כרטיסים שהמלאי שלהם נמוך מסף ההתראה"

---

### 3.2 אנליזות מכירות (יום/חודש/שנה) ✅
**תיאור:** להוסיף אפשרות לבחור תקופת זמן לאנליזת מכירות (יום/חודש/שנה)

**משימות:**
- [x] הוספת Select/Dropdown לבחירת תקופה (יום/שבוע/חודש/שנה)
- [x] עדכון `SalesChart` לתמוך בתקופות שונות
- [x] עדכון חישובי סטטיסטיקות לפי תקופה נבחרת
- [x] עדכון כותרת הגרף לפי התקופה

**קבצים לשינוי:**
- `src/pages/Dashboard.jsx` - הוספת בחירת תקופה
- `src/components/dashboard/SalesChart.jsx` - תמיכה בתקופות שונות

**הושלם:**
- ✅ הוספת 4 כפתורי בחירת תקופה: יום / שבוע / חודש / שנה
- ✅ עדכון SalesChart לתמוך בכל התקופות:
  - יום: 24 שעות אחרונות (לפי שעה)
  - שבוע: 7 ימים אחרונים (לפי יום)
  - חודש: 30 ימים אחרונים (לפי יום)
  - שנה: 12 חודשים אחרונים (לפי חודש)
- ✅ עדכון כותרת הגרף דינמית לפי התקופה הנבחרת
- ✅ עיצוב: כפתור פעיל עם רקע כחול, לא פעיל עם outline

**תצוגה:**
```
מכירות השבוע [יום] [שבוע] [חודש] [שנה]
[גרף...]
```

---

## 4. היסטוריית מכירות

### 4.1 הצגת הערות בעסקה ✅
**תיאור:** להוסיף אפשרות לצפות בהערה (notes) במידה וקיימת בעסקה

**משימות:**
- [x] עדכון `SalesHistory.jsx` - הצגת הערות בטבלה (אם קיימות)
- [x] עדכון Dialog של פרטי עסקה - הצגת הערות (כבר קיים, לבדוק)
- [x] הוספת אייקון/סמן לעסקאות עם הערות

**קבצים לשינוי:**
- `src/pages/SalesHistory.jsx` - בדיקה והשלמה של הצגת הערות

**הושלם:**
- ✅ הוספת עמודה "הערות" בטבלת היסטוריית מכירות
- ✅ הצגת אייקון FileText וטקסט הערות (מקוצר) בטבלה
- ✅ שיפור הצגת הערות ב-Dialog - עיצוב עם רקע כחול וכותרת
- ✅ אם אין הערות, מוצג "—" בטבלה

---

### 4.2 הצגת הערות ביומן פעילות ✅
**תיאור:** להוסיף אפשרות לצפות בהערות גם ביומן הפעילות

**משימות:**
- [x] עדכון `AuditLog.jsx` - הצגת הערות/פרטים נוספים
- [x] הוספת Dialog או הרחבה של רשומה להצגת פרטים מלאים
- [x] בדיקה שהערות נשמרות ב-AuditLog (בשדה `details`)

**קבצים לשינוי:**
- `src/pages/AuditLog.jsx` - הוספת הצגת הערות/פרטים

**הושלם:**
- ✅ הוספת כפתור "הצג פרטים" / "הסתר פרטים" לכל רשומה עם הערות/פרטים
- ✅ הצגת הערות (notes) ו-details בהרחבה של הרשומה
- ✅ הוספת כפתור "פרטים" שפותח Dialog עם כל הפרטים המלאים
- ✅ Dialog מציג: פעולה, תאריך, משתמש, סיבה, הערות, ופרטים נוספים (JSON)
- ✅ עיצוב: הערות ברקע כחול, סיבה ברקע צהוב, פרטים ברקע אפור

---

## 5. הוספת משתמש חדש

### 5.1 שינוי מונחים (זכיין/עוזר זכיין) ✅
**תיאור:** לשנות את המונחים מ-"בעל עסק" ו-"עובד" ל-"זכיין" ו-"עוזר זכיין"

**משימות:**
- [x] עדכון `positionLabels` ב-`UsersManagement.jsx`
- [x] עדכון כל הטקסטים הקשורים לתפקידים
- [x] עדכון אייקונים/צבעים אם נדרש

**קבצים לשינוי:**
- `src/pages/UsersManagement.jsx` - שינוי כל המונחים
- `src/pages/Layout.jsx` - עדכון תצוגת משתמש

**הושלם:**
- ✅ שינוי `positionLabels`: owner -> "זכיין", seller -> "עוזר זכיין"
- ✅ עדכון כל הטקסטים ב-UsersManagement.jsx
- ✅ עדכון תצוגת תפקיד ב-Layout.jsx
- ✅ עדכון תיאור העמוד: "ניהול זכיינים ועוזרי זכיין"

---

### 5.2 ניהול סיסמאות ✅
**תיאור:** לטפל בשאלות:
- האם בעל עסק מנפיק סיסמה?
- האם בעל עסק יכול לשחזר/לחדש סיסמה?

**משימות:**
- [x] הוספת כפתור "איפוס סיסמה" בעריכת משתמש
- [x] יצירת פונקציה לאיפוס סיסמה (Firebase Admin SDK או REST API)
- [x] הוספת Dialog לאיפוס סיסמה עם אישור
- [x] הוספת אפשרות להצגת סיסמה זמנית (אם נדרש)
- [x] עדכון UI - הסבר על ניהול סיסמאות

**קבצים לשינוי:**
- `src/pages/UsersManagement.jsx` - הוספת כפתור איפוס סיסמה
- `src/firebase/services/auth.js` - הוספת פונקציה לאיפוס סיסמה
- `src/api/firebaseClient.js` - הוספת פונקציה לאיפוס סיסמה

**הושלם:**
- ✅ הוספת כפתור "איפוס סיסמה" בעריכת משתמש (עם אייקון KeyRound)
- ✅ יצירת פונקציה `resetUserPassword` ב-auth.js המשתמשת ב-Firebase REST API
- ✅ הוספת Dialog אישור לאיפוס סיסמה עם הסבר מפורט
- ✅ Dialog מציג את כתובת האימייל אליה תישלח הודעת איפוס
- ✅ שימוש ב-Firebase REST API (sendOobCode) לשליחת הודעת איפוס סיסמה
- ✅ המשתמש יקבל אימייל עם קישור לאיפוס סיסמה

**הערה:** איפוס סיסמה ב-Firebase דורש שימוש ב-Admin SDK או REST API עם API key. ✅ מימוש באמצעות REST API.

---

## 6. עיצוב

### 6.1 התאמת עיצוב ✅
**תיאור:** לבדוק אפשרות להתאים את העיצוב הקיים

**משימות:**
- [x] יצירת קומפוננטה `ThemeSettings` או דף הגדרות
- [x] הוספת אפשרות לבחירת צבעים/תמה
- [x] שמירת העדפות ב-localStorage או Firebase
- [x] יישום תמה דינמית

**קבצים לשינוי:**
- יצירת `src/pages/Settings.jsx` (חדש)
- `src/App.jsx` - יישום תמה
- `src/index.css` - משתני CSS דינמיים

**הושלם:**
- ✅ יצירת דף הגדרות מלא (`Settings.jsx`)
- ✅ הוספת אפשרות לבחירת צבע ראשי (6 אפשרויות: כחול-סגול, ירוק, כתום, כחול, ורוד, סגול)
- ✅ הוספת אפשרות לבחירת תמה (בהיר/כהה)
- ✅ הוספת אפשרות לבחירת גודל גופן (קטן/בינוני/גדול)
- ✅ שמירת העדפות ב-localStorage
- ✅ יישום תמה דינמית ב-App.jsx (טעינה אוטומטית בעת טעינת האפליקציה)
- ✅ הוספת דף ההגדרות לתפריט הניווט
- ✅ כפתור איפוס הגדרות לברירת מחדל
- ✅ עיצוב יפה עם תצוגה מקדימה של הצבעים והתמות

**פיצ'רים:**
- ✅ בחירת צבע ראשי (6 אפשרויות)
- ✅ בחירת תמה (בהיר/כהה)
- ✅ התאמת גופנים (3 גדלים)

---

### 6.2 המרה לאפליקציה (PWA)
**תיאור:** להפוך את המערכת לאפליקציה (PWA - Progressive Web App)

**משימות:**
- [ ] יצירת `manifest.json` עם הגדרות האפליקציה
- [ ] יצירת Service Worker ל-offline support
- [ ] הוספת אייקונים בגדלים שונים
- [ ] הוספת meta tags ל-`index.html`
- [ ] בדיקת התקנה על מכשירים שונים
- [ ] הוספת splash screen

**קבצים לשינוי/יצירה:**
- `public/manifest.json` (חדש)
- `public/sw.js` או `src/sw.js` (חדש)
- `index.html` - הוספת links ל-manifest
- `vite.config.js` - הגדרות PWA plugin

**הערה:** ניתן להשתמש ב-`vite-plugin-pwa` להקלה על התהליך.

---

## 7. כללי

### 7.1 רמת אבטחה
**תיאור:** לבדוק ולתעד את רמת האבטחה הקיימת

**משימות:**
- [ ] סקירת כללי Firebase Security Rules
- [ ] בדיקת אימות משתמשים (Authentication)
- [ ] בדיקת הרשאות (Authorization)
- [ ] יצירת מסמך `SECURITY.md` עם תיעוד אבטחה
- [ ] בדיקת HTTPS/SSL
- [ ] בדיקת הגנה מפני XSS, CSRF

**קבצים לבדיקה:**
- Firebase Security Rules (אם קיים קובץ)
- `src/firebase/services/*.js` - בדיקת validation
- `src/api/firebaseClient.js` - בדיקת authentication

**תיעוד מוצע:**
- רשימת כללי אבטחה
- רמת גישה לכל collection
- המלצות לשיפור

---

### 7.2 הגבלת מספר משתמשים
**תיאור:** להגביל כל זכיין לעד 4 משתמשים (כולל בעלים), עם התראה בתשלום

**משימות:**
- [ ] הוספת בדיקה לפני יצירת משתמש חדש
- [ ] ספירת משתמשים פעילים
- [ ] הוספת התראה כאשר מגיעים ל-4 משתמשים
- [ ] הוספת Dialog עם הודעה על תשלום
- [ ] הוספת שדה `user_limit` ב-User או ב-settings נפרד

**קבצים לשינוי:**
- `src/pages/UsersManagement.jsx` - הוספת בדיקה והתראה
- `src/firebase/services/users.js` - פונקציה לספירת משתמשים

**לוגיקה:**
```javascript
const MAX_FREE_USERS = 4;

const checkUserLimit = async () => {
  const users = await User.list();
  const activeUsers = users.filter(u => u.is_active !== false);
  if (activeUsers.length >= MAX_FREE_USERS) {
    // הצגת התראה
    return false;
  }
  return true;
};
```

---

### 7.3 התחברות דרך Google
**תיאור:** להציע התחברות אוטומטית דרך Gmail/Google

**משימות:**
- [ ] הגדרת Google OAuth ב-Firebase Console
- [ ] הוספת כפתור "התחבר עם Google" בדף ההתחברות
- [ ] הוספת פונקציה ל-Google Sign-In
- [ ] עדכון `Login.jsx` עם כפתור Google
- [ ] בדיקת יצירת משתמש אוטומטית בכניסה ראשונה

**קבצים לשינוי:**
- `src/pages/Login.jsx` - הוספת כפתור Google
- `src/firebase/services/auth.js` - הוספת Google Sign-In
- `src/firebase/config.js` - בדיקת הגדרות

**הערה:** דורש הגדרה ב-Firebase Console - Authentication > Sign-in method > Google.

---

## סיכום משימות לפי עדיפות

### עדיפות גבוהה (Core Features):
1. ✅ הנפקת קוד אוטומטית (1.2)
2. ✅ הוספת כינוי (1.3)
3. ✅ סטטיסטיקות מלאי (1.5)
4. ✅ רובריקות מחיר במכירה (2.2)
5. ✅ שינוי מונחים לזכיין/עוזר זכיין (5.1)
6. ✅ הגבלת משתמשים (7.2)

### עדיפות בינונית:
7. ✅ סוג כרטיס (1.1)
8. ✅ ערכים מוגדרים מראש (1.4)
9. ✅ אנליזות מכירות (3.2)
10. ✅ ניהול סיסמאות (5.2)
11. ✅ הצגת הערות (4.1, 4.2)

### עדיפות נמוכה (Nice to Have):
12. ✅ הסבר מלאי קריטי (3.1)
13. ✅ התאמת עיצוב (6.1)
14. ✅ PWA (6.2)
15. ✅ Google Sign-In (7.3)
16. ✅ תיעוד אבטחה (7.1)

---

## הערות טכניות

### שינויים נדרשים ב-Firebase:
- הוספת שדות חדשים ל-`ticketTypes` collection:
  - `ticket_category` (string)
  - `nickname` (string, optional)
  - `default_quantity_per_package` (number)
- עדכון Security Rules אם נדרש

### שינויים נדרשים ב-UI:
- עדכון כל הטפסים עם שדות חדשים
- הוספת validation לשדות חדשים
- עדכון תצוגות להצגת מידע חדש

### בדיקות נדרשות:
- בדיקת יצירת כרטיס עם כל השדות החדשים
- בדיקת עריכת כרטיס (read-only למחיר)
- בדיקת פילטרים חדשים
- בדיקת הגבלת משתמשים
- בדיקת Google Sign-In

---

## לוח זמנים מוצע

**שבוע 1:**
- מלאי: קוד אוטומטית, כינוי, סטטיסטיקות
- משתמשים: שינוי מונחים, הגבלת משתמשים

**שבוע 2:**
- מכירה: רובריקות מחיר, שינוי שם
- לוח בקרה: אנליזות, הסבר מלאי קריטי

**שבוע 3:**
- היסטוריית מכירות: הערות
- משתמשים: ניהול סיסמאות
- כללי: Google Sign-In

**שבוע 4:**
- עיצוב: התאמה, PWA
- תיעוד: אבטחה
- בדיקות ופינס

---

*מסמך זה עודכן לאחרונה: [תאריך]*

